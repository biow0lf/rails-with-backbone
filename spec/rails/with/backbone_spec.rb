require "spec_helper"

RSpec.describe Rails::With::Backbone do
  it "has a version number" do
    expect(Rails::With::Backbone::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
